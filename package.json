{
  "name": "holvi-agent-bridge",
  "version": "0.1.0",
  "description": "A capability-scoped local agent bridge for Holvi through a signed-in Chrome session.",
  "private": true,
  "type": "module",
  "bin": {
    "holvi-agent-bridge": "./dist/native/cli.js"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsgo -p tsconfig.native.json && tsgo -p tsconfig.extension.json && cp src/extension/manifest.json dist/extension/manifest.json",
    "check": "node scripts/check.mjs",
    "format": "oxfmt --write .",
    "pretest": "npm run build",
    "test": "node --test test/*.test.ts"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.329",
    "@types/node": "^22.0.0",
    "@typescript/native-preview": "^7.0.0-dev.20260707.2",
    "oxfmt": "0.56.0",
    "oxlint": "1.71.0",
    "oxlint-tsgolint": "0.23.0"
  }
}
